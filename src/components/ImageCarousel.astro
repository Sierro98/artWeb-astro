---
import { Image } from 'astro:assets';

type Props = {
    destination:string;
}

const {destination} = Astro.props;
const imgDescription: string = "JavatoGarabato Art"
const imgWidth = 400;
const imgHeight = 350;

import firstImg from "../assets/MainCarousel/Andrew Kliatskyi 3D Render.jpg";
import secondImg from "../assets/MainCarousel/Javier Miranda.jpg";
import thirdImg from "../assets/MainCarousel/Luke Jones 3D Render.jpg";
import fourthImg from "../assets/MainCarousel/Marco Damm 3D Render.jpg";
import fifthImg from "../assets/MainCarousel/Rick Rothenberg 3D Renders.jpg";
---
<div class="pb-12">
    <a href={destination} class="scroll">
        <div class="scroll-slide">
            <Image format='avif' quality={'max'} src={firstImg} alt={imgDescription} width={imgWidth} height={imgHeight}/>
            <Image format='avif' quality={'max'} src={secondImg} alt={imgDescription} width={imgWidth} height={imgHeight}/>
            <Image format='avif' quality={'max'} src={thirdImg} alt={imgDescription} width={imgWidth} height={imgHeight}/>
            <Image format='avif' quality={'max'} src={fourthImg} alt={imgDescription} width={imgWidth} height={imgHeight}/>
            <Image format='avif' quality={'max'} src={fifthImg} alt={imgDescription} width={imgWidth} height={imgHeight}/>
        </div>
        <div class="scroll-slide">
            <Image format='avif' quality={'max'} src={firstImg} alt={imgDescription} width={imgWidth} height={imgHeight}/>
            <Image format='avif' quality={'max'} src={secondImg} alt={imgDescription} width={imgWidth} height={imgHeight}/>
            <Image format='avif' quality={'max'} src={thirdImg} alt={imgDescription} width={imgWidth} height={imgHeight}/>
            <Image format='avif' quality={'max'} src={fourthImg} alt={imgDescription} width={imgWidth} height={imgHeight}/>
            <Image format='avif' quality={'max'} src={fifthImg} alt={imgDescription} width={imgWidth} height={imgHeight}/>
        </div>
    </a>
</div>

<style>
.scroll {
    display: block;
    position: relative;
    width: 100%;
    height: 350px;
    background-color: transparent;
    overflow: hidden !important;
    white-space: nowrap;
    z-index: 1;
}
.scroll-slide {
    animation: scrollImage 50s infinite linear;
    display: inline-block;
    justify-content: space-between;
    align-items: center;
    position: relative;
    white-space: nowrap;
}
.scroll img {
    height: 350px;
    min-width: 400px;
	object-fit: cover;
    display: inline-block;
    margin: 0 20px;
    color: white;
}
@keyframes scrollImage {
    from {
        transform: translateX(-100%);
    }
    to {
        transform: translateX(0%);
    }
}
</style>