---
import {Image} from "astro:assets"

const userEmail = Astro.cookies.get("userEmail")
---

<header
    class="flex flex-col py-12 px-24 max-sm:px-5 max-sm:py-10 max-w-7xl justify-self-center w-full"
>   
    <div class="flex justify-between items-center">
        <div class="flex items-end">
            <h1 class="text-4xl">JavatoGarabato</h1>
            <p class="px-3 text-xs">Artista desde 1998</p>
        </div>
        
        <a href="/" class="flex gap-x-4 items-center">
            <Image
                src="/heartbeat.png"
                alt="rhythm nation logo"
                width={64}
                height={64}
                format="webp"
                class="w-16"
            />
        </a>
    </div>

    <hr class="flex-grow border-gray-800"></hr>

    <div class="flex justify-between items-center py-3 text-lg font-semibold px-4">
        <p>Obras</p>
        <p>Blog</p>
        <p>Productos</p>
        <p>Contacto</p>
    </div>

    <hr class="flex-grow border-gray-800"></hr>
    
</header>

<style>
    h1 {
        font-family: 'Azeret Mono Variable', monospace;
        font-weight: bold;
    }
    p {
        font-family: 'Azeret Mono Variable', monospace;
    }
</style>